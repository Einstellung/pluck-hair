_base_: ../base/training_defaults.yaml

experiment:
  name: rtdetr_l_dota8_smoke
  output_dir: experiments

detector:
  type: yolo
  runner: yolo
  config:
    model: rtdetr-l.pt
    data_yaml: dataset/dota8/dota8.yaml
    device: 0

training:
  epochs: 1
  batch: 4
  imgsz: 640
  device: 0
  project: experiments
  name: rtdetr_l_dota8_smoke
