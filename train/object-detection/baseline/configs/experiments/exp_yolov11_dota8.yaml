_base_: ../base/training_defaults.yaml

experiment:
  name: yolov11_dota8_smoke
  output_dir: experiments

detector:
  type: yolo
  runner: yolo
  config:
    model: yolo11n.pt  # 修正：YOLO11 没有 'v'
    data_yaml: dataset/dota8/dota8.yaml
    device: 0

training:
  epochs: 1
  batch: 4
  imgsz: 640
  device: 0
  project: experiments
  name: yolov11_dota8_smoke
